<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.matchmaker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".PlayerOnePlaceShips" />
        <activity android:name=".AuthenticationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlayerTwoPlaceShips" />
        <activity android:name=".PlayerOneMoves" />
        <activity android:name=".PlayerTwoMoves" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".LoserActivity" />
        <activity android:name=".WinnerActivity" />
        <activity android:name=".CloserToWinning"></activity>
    </application>

</manifest>